/* -------------------------------------------------
 * Nextflow config file for MENDEL
 * -------------------------------------------------
 */

process {
  executor = 'pbs'
  beforeScript = 'module load Miniconda3'
  module = ['Singularity']
}
